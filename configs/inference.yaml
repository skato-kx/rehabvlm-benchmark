# Inference / evaluation configuration scaffold
model:
  checkpoint_path: "outputs/checkpoints/best"
  base_model: "qwen-vl"

data:
  test_annotations_file: "data/annotations/test.jsonl"
  raw_video_dir: "data/raw"

inference:
  device: "cuda"
  max_length: 128
  num_choices: 4
